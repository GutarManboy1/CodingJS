// Given a non-empty string like 'Code' return a string lik 'CCoCodCode'

function stringSplosion(str){
    return str.repeat(1, str.length-1)
  }

  //this is the answer i came up with and it accepts but doesn't provide test examples. 